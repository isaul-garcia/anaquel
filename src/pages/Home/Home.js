import React from 'react';
import { Dashboard } from '../../components';

const Home = (click) => {
    return (
        <>
            <Dashboard click={click} />
        </>
    )
}

export default Home;